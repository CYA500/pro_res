name: pro_res
description: AuraMonitor Pro — Real-time PC monitoring with a cyberpunk UI.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.1
  mobile_scanner: ^5.1.1
  google_fonts: ^6.2.1
  vibration: ^1.8.4
  provider: ^6.1.2
  http: ^1.2.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.0
  fl_chart: ^0.67.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
